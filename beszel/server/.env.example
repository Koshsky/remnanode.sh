# Переменные для Beszel hub (передаются в docker compose).
# cp .env.example .env, при необходимости меняем значения.

# Порт веб-интерфейса hub (наружу). По умолчанию Beszel слушает 8090.
# HUB_GUI_PORT="8090"
