# Скопировать в group_vars/all/vars.yml (файл в .gitignore) и заполнить.
# hostname ноды берётся из имени хоста в инвентаре;
# DOMAIN = <hostname>.<DOMAIN_ZONE> (можно переопределить через host_vars).

# --- обязательные ---
NEW_USER_LOGIN: user
NEW_USER_PASSWORD: ""
# ВАЖНО: RemnaWave выдаёт ПО-НОДНЫЙ секрет (внутри зашиты сертификаты ноды) —
# для парка задавайте его в inventory/host_vars/<node>.yml (REMNAWAVE_SECRET_KEY),
# а здесь оставьте ключ первой ноды или пустым. Общий ключ на все ноды =
# «timeout of 15000ms exceeded» в панели.
REMNAWAVE_SECRET_KEY: ""

# --- опционально ---
# Почта для ACME-аккаунта Caddy (необязательна при http-01; можно оставить пустой)
EMAIL: you@example.com

# --- сеть / домен ---
# Зона по умолчанию для всего парка. Если у нод разные зоны/домены —
# задайте DOMAIN (или DOMAIN_ZONE) в host_vars/<host>.yml — это перекроет значение.
DOMAIN_ZONE: example.com            # node-01.example.com
DOMAIN: "{{ inventory_hostname }}.{{ DOMAIN_ZONE }}"   # auto: node-01.example.com
SSH_PORT: 22                        # ssh всегда на стандартном 22
XRAY_PORT: 443
REMNANODE_PORT: 8443
CADDY_FALLBACK_PORT: 8445           # порт фоллбэка xray → Caddy (landing/health; dest в панели RemnaWave: 127.0.0.1:8445, proxyProtocol off)

# --- опции ---
AUTO_REBOOT: ""                    # daily | weekly | "" (выкл)

# Автообновления: true — только security-паттерны (по умолчанию); false — полностью выключить
UNATTENDED_UPGRADES: true
# Email для уведомлений unattended-upgrades (пусто — выкл; требует MTA на хосте)
UNATTENDED_UPGRADES_MAIL: ""

# Контрольная машина: статический IP для fail2ban ignoreip (если SSH_CONNECTION на нодах не виден)
ANSIBLE_CONTROL_IP: ""

# Beszel-агент (если HUB_URL пуст — агент не ставится).
# Ключ/токен Beszel выдаются на ОДНУ систему (Add System) — для нескольких нод
# задавайте BESZEL_AGENT_KEY/BESZEL_TOKEN в inventory/host_vars/<node>.yml.
BESZEL_HUB_URL: "https://dashboard.example.com"
BESZEL_AGENT_KEY: ""               # из Web-UI hub'а (Add System)
BESZEL_TOKEN: ""
BESZEL_AGENT_PORT: 45876
BESZEL_ALLOW_FROM: ""              # IP/подсеть hub'а — открыть 45876 в UFW (SSH-режим)