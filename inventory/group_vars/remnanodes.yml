# Единый ключ парка: все ноды авторизуют один pub (playbooks/keys/park/id_ed25519.pub).
# Работает для ad-hoc и playbook'ов из ЛЮБОГО каталога (inventory_dir-относительно).
ansible_ssh_private_key_file: "{{ inventory_dir | default('') }}/../playbooks/keys/park/id_ed25519"
